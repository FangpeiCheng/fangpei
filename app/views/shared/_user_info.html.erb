<%= link_to gravatar_for(@user, size: 50), user_path(@user) %>
     <h1><%= @user.name %></h1>
   
 <span ><%= pluralize(@user.microposts.count, "microposts") %></span>



 <!--<%#= link_to gravatar_for(current_user, size: 50), user_path(current_user) %>
     <h1><%#= current_user.name %></h1>
   
 <span ><%#= pluralize(current_user.microposts.count, "microposts") %></span>-->

    